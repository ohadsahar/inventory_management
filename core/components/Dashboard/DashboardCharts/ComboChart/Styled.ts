import styled from 'styled-components';

export const ComboChartWrapper = styled.div`
  width: 50vw;
  background-color: white;
  border-radius: 8px;
  padding: 24px;
  box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
  @media (max-width: 768px) {
    height: 300px;
  }
`;
